query GetWorkItems {
  group(fullPath: "org") {
    workItems {
      nodes {
        id
        iid
        title
        state
        webUrl
        createdAt
        updatedAt
      }
    }
  }
}
